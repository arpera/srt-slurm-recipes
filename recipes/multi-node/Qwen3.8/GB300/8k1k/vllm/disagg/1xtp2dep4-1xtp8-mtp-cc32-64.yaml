name: "qwen38-2.4t-nvfp4-gb300-disagg-1xtp2dep4-1xtp8-mtp-cc32-64"

identity:
  model:
    repo: "Inferact/Qwen3.8-2.4T-A95B-NVFP4"
    revision: "a6abb8bfb82b6965166d4129275f012d8809bc40"
  container:
    image: "vllm/vllm-openai:nightly-a9a17e7095a66ef6c6685a1c7ddd657781a78d3c"
  frameworks:
    dynamo: "1.2.0.dev20260526"
    vllm: "0.26.1rc1.dev1177+ga9a17e709"

slurm:
  time_limit: "1:00:00"

model:
  path: "hf:Inferact/Qwen3.8-2.4T-A95B-NVFP4"
  container: "vllm/vllm-openai:nightly-a9a17e7095a66ef6c6685a1c7ddd657781a78d3c"
  precision: "NVFP4"

dynamo:
  version: "1.2.0.dev20260526"
  install: false

resources:
  gpu_type: "gb300"
  gpus_per_node: 4

  prefill_nodes: 2
  prefill_workers: 1
  gpus_per_prefill: 8

  decode_nodes: 2
  decode_workers: 1
  gpus_per_decode: 8

frontend:
  type: dynamo
  enable_multiple_frontends: false

backend:
  type: vllm
  connector: null

  prefill_environment:
    VLLM_SSM_CONV_STATE_LAYOUT: "DS"

    # TODO: remove these
    DYN_LOG: "error"
    DYN_SDK_DISABLE_ANSI_LOGGING: "1"
    VLLM_LOGGING_COLOR: "0"

  decode_environment:
    VLLM_SSM_CONV_STATE_LAYOUT: "DS"

    # TODO: remove these
    DYN_LOG: "error"
    DYN_SDK_DISABLE_ANSI_LOGGING: "1"
    VLLM_LOGGING_COLOR: "0"

  vllm_config:
    prefill:
      # TODO: check it might be enabled by default
      no-disable-hybrid-kv-cache-manager: true

      # TODO: remove this
      async-scheduling: true

      # prefill topology:
      # TP2DP4+EP
      tensor-parallel-size: 2
      data-parallel-size: 4
      enable-expert-parallel: true

      kv-transfer-config: '{"kv_connector": "NixlConnector", "kv_role": "kv_both", "kv_load_failure_policy": "fail"}'
      generation-config: "vllm"

      # random dataset benchmark shound not use prefix caching
      no-enable-prefix-caching: true

      # save kv cache and utilize kernel fusion
      language-model-only: true

      # Full-Attn state
      kv-cache-dtype: "fp8"

      # GDN state
      mamba-ssm-cache-dtype: "bfloat16"

      # must be > ISL+OSL+chat_template_len
      max-model-len: 10240

      # must have setting to bound cuda graph sizes to avoid OOM
      # this setting should NOT affect prefill performance
      # 32 is arbitrary number here
      max-num-seqs: 32

      # 2xISL
      max-num-batched-tokens: 16384

      compilation-config: '{"cudagraph_mode": "PIECEWISE"}'

      # IMPORTANT:
      # This is NOT a default value for all2all-backend
      # The default value is "allgather_reducescatter"
      # This value was changed to "flashinfer_nvlink_one_sided"
      # because it shows E2E perf improvement
      all2all-backend: "flashinfer_nvlink_one_sided"

      # enable MTP because because decode side requires it
      speculative_config.method: "mtp"
      speculative_config.num_speculative_tokens: 3

      # IMPORTANT!!!
      # must match the decode side, which is TP8 and would otherwise settle on
      # 1088 against this side's 2176; NixlConnector then refuses the handshake
      # and retries per request for the whole run
      block-size: 2176

    decode:
      # TODO: check it might be enabled by default
      no-disable-hybrid-kv-cache-manager: true

      # decode topology:
      # TP8+EP
      tensor-parallel-size: 8
      data-parallel-size: 1
      enable-expert-parallel: true

      # TODO: remove this
      async-scheduling: true

      kv-transfer-config: '{"kv_connector": "NixlConnector", "kv_role": "kv_both", "kv_load_failure_policy": "fail"}'

      generation-config: "vllm"

      # random dataset benchmark shound not use prefix caching
      no-enable-prefix-caching: true

      # save kv cache and utilize kernel fusion
      language-model-only: true

      # Full-Attn state
      kv-cache-dtype: "fp8"

      # GDN state
      mamba-ssm-cache-dtype: "bfloat16"

      # must be > ISL+OSL+chat_template_len
      max-model-len: 10240

      max-num-seqs: 80                 # (max_cc/DP1) + eps = 64/1 + 16 = 80
      max-cudagraph-capture-size: 320  # max-num-seqs * (1 + MTP) = 80 * (1 + 3) = 320
      max-num-batched-tokens: 336      # max-cudagraph-capture-size + eps = 320 + 16 = 336
      compilation-config: '{"cudagraph_mode": "FULL_DECODE_ONLY"}'

      # IMPORTANT:
      # This is NOT a default value for all2all-backend
      # The default value is "allgather_reducescatter"
      # This value was changed to "flashinfer_nvlink_one_sided"
      # because it shows E2E perf improvement
      all2all-backend: "flashinfer_nvlink_one_sided"

      # MTP on
      # this requires extra space in kv cache
      speculative_config.method: "mtp"
      speculative_config.num_speculative_tokens: 3

      # IMPORTANT!!!
      # must match the prefill side
      block-size: 2176

benchmark:
  type: custom
  env:
    AIPERF_CONCURRENCIES: "32x64"
    AIPERF_MODEL: "Inferact/Qwen3.8-2.4T-A95B-NVFP4"
  command: |
    set -euo pipefail
    command -v aiperf >/dev/null 2>&1 || pip install --quiet aiperf
    URL="${SRT_FRONTEND_HOST}:${SRT_FRONTEND_PORT}"
    ISL=8192
    OSL=1024
    NUM_PROMPTS_MULT=8
    NUM_WARMUP_MULT=1
    IFS='x' read -ra CC_LIST <<< "${AIPERF_CONCURRENCIES}"
    for CC in "${CC_LIST[@]}"; do
      NUM_PROMPTS=$((CC * NUM_PROMPTS_MULT))
      WARMUP=$((CC * NUM_WARMUP_MULT))
      ARTIFACT_DIR="/logs/aiperf_isl_${ISL}_osl_${OSL}/concurrency_${CC}"
      mkdir -p "$ARTIFACT_DIR"
      aiperf profile \
        --model "${AIPERF_MODEL}" \
        --tokenizer "${AIPERF_MODEL}" \
        --url "$URL" \
        --artifact-dir "$ARTIFACT_DIR" \
        --profile-export-file profile_export_aiperf.json \
        --endpoint-type chat \
        --endpoint /v1/chat/completions \
        --streaming \
        --ui-type None \
        --isl "$ISL" \
        --osl "$OSL" \
        --use-server-token-count \
        --tokenizer-trust-remote-code \
        --extra-inputs 'ignore_eos:true' \
        --prompt-corpus random \
        --random-range-ratio 1.0 \
        --concurrency "$CC" \
        --request-count "$NUM_PROMPTS" \
        --warmup-request-count "$WARMUP"
    done
